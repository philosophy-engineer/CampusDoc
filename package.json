{
  "name": "campus-doc",
  "version": "0.1.0",
  "description": "CampusDoc Electron v1 (TXT workspace)",
  "main": "electron/main.js",
  "license": "MIT",
  "scripts": {
    "dev": "cross-env CAMPUSDOC_DEV=1 electron .",
    "start": "electron .",
    "test": "vitest run --globals",
    "test:watch": "vitest --globals",
    "dist": "electron-builder --publish never",
    "dist:win": "electron-builder --win portable --publish never",
    "dist:mac": "electron-builder --mac dmg --publish never"
  },
  "engines": {
    "node": "24.x"
  },
  "devDependencies": {
    "cross-env": "^10.1.0",
    "electron": "^41.2.0",
    "electron-builder": "^26.8.1",
    "vitest": "^4.1.3"
  },
  "build": {
    "appId": "com.campusdoc.desktop",
    "productName": "CampusDoc",
    "asar": true,
    "directories": {
      "output": "dist"
    },
    "files": [
      "app.js",
      "index.html",
      "files/**/*",
      "electron/**/*",
      "package.json"
    ],
    "mac": {
      "target": [
        "dmg"
      ],
      "category": "public.app-category.productivity"
    },
    "win": {
      "target": [
        {
          "target": "portable",
          "arch": [
            "x64"
          ]
        }
      ]
    }
  }
}
